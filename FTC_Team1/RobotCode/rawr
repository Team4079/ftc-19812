//yo what is up guys today we are going to be checking out this secret code we found, so lets begin yah?

package RobotCode;

import com.qualcomm.robotcore.eventloop.opmode.OpMode;
import com.qualcomm.robotcore.util.ElapsedTime;
import com.qualcomm.robotcore.eventloop.opmode.TeleOp;
import com.qualcomm.robotcore.hardware.DcMotor;
import com.qualcomm.robotcore.hardware.Servo;
import com.qualcomm.robotcore.hardware.CRServo;

@TeleOp(name="rishi", group="Iterative Opmode")

public class RishiSecret extends OpMode {
    private DcMotor slideMotor;
    private double slidePower;
    private double Kp = 0.3;
    private double Ki = 0.1;
    private double Kd = 0.2;
    private double reference = 10;
    private double integralsum = 0;
    private double lastError = 0;

    ElapsedTime rishiTime = new ElapsedTime();

@Override
public void init() {
    slideMotor = hardwareMap.get(DcMotor.class, "slideMotor");
}

@Override
public void loop() {
    while (test) {

    }
    slideMotor.setPower(slidePower);
}
}
